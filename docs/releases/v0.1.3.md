## v0.1.3 更新

- 截图选区底图、放大镜、预览、OCR、复制和保存共用截图启动时冻结的桌面画面，避免选择完成时前台弹层消失、结果变成下层窗口。
- 修复 Windows 多屏混合 DPI 下的坐标错位，以及分数缩放下的边缘裁剪越界。所有屏幕底图准备完成后再显示选区。
- 修复截图快捷键失效、错误状态无法按 Esc 退出，以及切换截图会话时旧会话误关正在使用的窗口。
- 改善 macOS 系统 OCR 对空结果和低置信度图像的恢复，并在服务设置中标明本地处理或发送到服务。
- 防止较早的 OCR／翻译请求覆盖新内容，完善翻译失败提示和重试处理。
- 菜单快捷键随设置同步更新，支持自定义、清除和重置。

## 下载与安装

- macOS 14 或更新：Apple Silicon 下载 `aarch64.dmg`；Intel 下载 `x64.dmg`。拖入“应用程序”后启动。
- Windows x64：优先下载 `x64-setup.exe`（当前用户安装）；MSI 为备选安装器，不必重复安装。
- Windows 缺少 WebView2 时，普通安装器需要联网。本次默认安装包不包含离线 WebView2 运行时。

## 签名与权限

macOS 使用 ad-hoc 签名，未经 Apple 公证。首次启动被拦截时，请在“系统设置 → 隐私与安全性”中选择“仍要打开”。截图需要屏幕录制权限；选中文本和界面元素检测需要辅助功能权限。ad-hoc 身份可能随构建变化，升级后系统隐私权限可能需要重新确认。

Windows 当前未使用公开信任的代码签名，可能显示未知发布者或 SmartScreen 提示。仅从本仓库下载并核对校验值。若设备策略或 Smart App Control 阻止运行，请联系设备管理员；不要关闭系统防护。

macOS / Windows 默认使用系统 OCR。Windows 需要安装对应的系统 OCR 语言能力；当前 macOS 官方构建不包含 Tesseract。

## 验证范围

多屏坐标、混合缩放、跨屏合成和冻结画面一致性已有自动化回归覆盖。本次本地截图验证使用单屏 Mac；真实双屏、macOS 独立显示器 Spaces／全屏应用，以及 Windows 桌面上的混合 DPI 交互尚未完成实机验收。自动构建和安装检查不代替这些交互验证。

## 校验与更新

每个平台附 `SHA256SUMS-*.txt` 和 `build-*.json`，记录版本、源码提交和签名方式。

- macOS：`shasum -a 256 下载的文件.dmg`
- Windows PowerShell：`Get-FileHash .\下载的文件.exe -Algorithm SHA256`

更新前退出 SnapLingo，覆盖安装同一架构版本。正常覆盖安装不应删除 API 配置或历史记录。
